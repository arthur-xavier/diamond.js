# src/model/storage/mongodb.coffee

Model = require '../../model'

class MongoDB extends Model

  constructor: ->
    super

  all: ->

  find: ->

  findById: ->

  save: ->

  remove: ->

  update: ->

module.exports = MongoDB
