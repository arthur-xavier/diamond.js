# test/app/controllers/testController.coffee

Controller = require "#{root}/controller"

class TestController extends Controller

  index: ->

  show: ->

  create: ->

  update: ->

  destroy: ->

  test: ->

  param: ->

module.exports = TestController
